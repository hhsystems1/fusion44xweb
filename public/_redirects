# Redirect rules for Fusion 44X
# Preserve existing URLs and handle potential old paths

# If any old URLs need to be redirected, add them here:
# /old-path  /new-path  301

# Force trailing slash behavior
/*  /:splat  200
